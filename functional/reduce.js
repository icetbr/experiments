const { adresses, newIds, errors } = ids.reduce(({ adresses, newIds, errors }, id) => {

}, { adresses: empresa.dados?.enderecos || [], newIds: empresa.dados?.DS_CNPJ || [], errors: [] })


const r = ids.reduce((r, id) => {

}, { adresses: empresa.dados?.enderecos || [], newIds: empresa.dados?.DS_CNPJ || [], errors: [] })

const r = ids.reduce((r, id) => {

}, {
    adresses: empresa.dados?.enderecos || [],
    newIds: empresa.dados?.DS_CNPJ || [],
    errors: [],
})
